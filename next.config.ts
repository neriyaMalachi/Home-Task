import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['via.placeholder.com'], // הוסף את הדומיין של התמונה החיצונית
  },
};

export default nextConfig;
